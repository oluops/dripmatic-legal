---
// Social Proof — hidden in v1 (no reviews yet), placeholder ready
const hasReviews = false;
---

{hasReviews && (
  <section class="py-20 sm:py-28 bg-surface-secondary">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 text-center">
      <p class="text-gold text-lg mb-4">⭐⭐⭐⭐⭐</p>
      <blockquote class="text-xl sm:text-2xl font-light italic mb-4">
        "Finally an app that actually knows my style."
      </blockquote>
      <p class="text-content-secondary text-sm">— Happy User</p>
    </div>
  </section>
)}
